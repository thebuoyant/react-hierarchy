export type AppConfigType = {
  layout: {
    card: {
      cardHeight: number;
      cardWidth: number;
      cardSpace: number;
      cardHeaderBackgroundColor: string;
      cardHeaderTitleColor: string;
    };
  };
};
